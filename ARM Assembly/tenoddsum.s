		AREA RESET, CODE, READONLY
ENTRY	
		MOV R1, #1; SUM
		MOV R2, #2; AUGEND OF 2
		MOV R3, #1; ODD NUMBER TO ADD
		MOV R4, #9; NUMBER OF TIMES TO ADD TO R1 TO GET FINAL RESULT
LOOP	ADD R3, R3, R2; ADDING 2 TO ODD NUMBER
		ADD R1, R1, R3; ADDING ODD NUMBER TO THE SUM
		SUBS R4, R4, #1; ONE ITERATION COMPLETED
		BNE LOOP
GO		B GO
		END